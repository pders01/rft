node_modules/
dist/
*.log
.DS_Store

# pnpm is the package manager; reject other lockfiles
package-lock.json
yarn.lock

# the real config and source/ are tracked in this repo

# sandbox install destination used by the example config
examples/
